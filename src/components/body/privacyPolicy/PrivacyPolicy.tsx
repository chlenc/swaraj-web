import React from "react";
import styled from "@emotion/styled";

const Root = styled.div`
display: flex;
justify-content: center;
`

const PrivacyPolicy: React.FC =() => <Root>
    Privacy Policy
</Root>

export default PrivacyPolicy;